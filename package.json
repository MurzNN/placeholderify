{
  "name": "placeholderify",
  "version": "1.0.0",
  "description": "A CSS library to make real HTML elements look like placeholders to simplify making placeholders of elements when the data is loading or reloading.",
  "repository": {
    "type": "git",
    "url": "https://github.com/MurzNN/placeholderify"
  },
  "keywords": [
    "placeholder",
    "placeholderify",
    "placeholify",
    "loader",
    "reloader",
    "skeleton",
    "progress"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/MurzNN/placeholderify/issues"
  },
  "homepage": "https://murznn.github.io/placeholderify/",
  "scripts": {
    "lint": "stylelint src dist",
    "build": "${npm_execpath} run lint && sass src/lib:dist && ${npm_execpath} run minify-css",
    "watch": "sass --watch src/lib:dist & nodemon --watch dist --ext css --ignore \"*.min.css\" --exec \"${npm_execpath} run minify-css\"",
    "minify-css": "find dist -name \"*.css\" -not -name \"*.min.css\" -exec sh -c 'for file; do ${npm_execpath} cleancss -o \"${file%.css}.min.css\" \"$file\"; done' sh {} +"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "nodemon": "^3.1.10",
    "sass": "^1.78.0",
    "stylelint": "^16.9.0",
    "stylelint-config-recommended-scss": "^15.0.1",
    "stylelint-config-standard": "^36.0.1",
    "stylelint-config-standard-scss": "^14.0.0",
    "stylelint-scss": "^6.12.0"
  }
}
